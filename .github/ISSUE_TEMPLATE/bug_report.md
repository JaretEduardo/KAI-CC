---
name: Compiler bug
about: Something in kaicc crashes, miscompiles, or disagrees with the docs
title: ""
labels: bug
---

**KAI-CC version** (`kaicc --version`):

**OS and architecture** (e.g. Linux x86_64, Windows x86_64):

**Installation method** (prebuilt release archive / VS Code extension / built
from source - only relevant if it might affect the bug):

**Minimal `.kai` source that reproduces it** (please paste text, not a
screenshot - a screenshot can't be copy-pasted to reproduce the bug):

```kai

```

**Exact command run:**

```sh
kaicc ...
```

**Expected behavior:**

**Actual behavior** (exact diagnostic text, program output, and/or exit code
- see [`docs/CLI.md`](../../docs/CLI.md) for exit code meanings):

**Does this reproduce on the latest release or on `master`?** (if known -
not required):
