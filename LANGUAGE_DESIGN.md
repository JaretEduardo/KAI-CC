# KAI Language Design

> Status: Draft  
> Target version: KAI 0.1

## 1. Overview

KAI is an AI-native compiled systems programming language designed for software that is efficient to generate, inspect, debug, and maintain by both humans and AI coding agents.

KAI aims to combine:

- compact and predictable source code
- static type safety
- native performance
- transparent memory behavior
- deterministic tooling
- structured compiler diagnostics
- semantic introspection
- low context and token overhead for AI agents

KAI is not designed to minimize characters at the cost of readability.

Instead, KAI attempts to minimize the amount of information that humans and AI agents must process in order to understand and safely modify a program.

---

## 2. Motivation

Modern AI coding agents frequently work with languages and ecosystems that were designed exclusively for human developers.

This creates several inefficiencies:

- large amounts of source code must be read to understand a project
- compiler errors are often verbose or unstructured
- dependency relationships are not always directly queryable
- multiple equivalent language constructs increase ambiguity
- implicit behavior makes automated reasoning harder
- code generation can require excessive boilerplate
- refactoring may require scanning large portions of a codebase

KAI explores what a systems programming language could look like if AI-assisted software development were considered from the beginning.

The compiler should not only translate source code into native programs.

It should also act as a semantic engine capable of explaining the program to development tools and AI agents.

---

## 3. Design Principles

### 3.1 Predictability

KAI should prefer one clear and canonical way to express common operations.

The language should avoid unnecessary syntax variants and hidden behavior.

### 3.2 Token Efficiency

KAI source code should remain compact without becoming cryptic.

Token efficiency should come from:

- concise syntax
- type inference
- minimal boilerplate
- strong module boundaries
- compiler-provided semantic information
- structured tooling

Token efficiency must be measured rather than assumed.

Future benchmarks should compare equivalent programs written in KAI and other languages using multiple model tokenizers.

### 3.3 Static Safety

KAI should catch as many programming errors as reasonably possible during compilation.

The initial language will use static typing.

Implicit conversions should be limited and predictable.

### 3.4 Native Performance

KAI is a compiled language.

Programs should compile to native machine code without requiring a virtual machine.

The reference compiler will initially target LLVM.

### 3.5 Transparent Behavior

Developers should be able to inspect what the compiler is doing.

KAI tooling should eventually expose:

- inferred types
- symbol references
- dependency relationships
- generated IR
- generated assembly
- memory information
- optimization information

### 3.6 AI-First Diagnostics

Compiler diagnostics should be useful to both humans and machines.

Each diagnostic should have:

- stable diagnostic code
- source location
- concise description
- relevant types or values
- machine-readable representation

Example:

    E0102
    src/main.kai:12:8

    expected: i32
    found: str

Structured output should eventually be available through:

    kai check --json

### 3.7 Maintainability

KAI should favor long-term readability over cleverness.

Code generated by an AI agent should still be understandable and maintainable by a human developer.

### 3.8 Systems-Level Capability

KAI should eventually allow low-level programming when required.

The language should not hide the underlying machine completely.

Future versions may expose:

- pointers
- explicit memory regions
- SIMD
- native calling conventions
- hardware-specific operations

These capabilities should not complicate ordinary KAI programs.

---

## 4. Memory Philosophy

KAI will not require a tracing garbage collector.

The long-term goal is deterministic resource management.

The exact ownership and borrowing model is not yet finalized.

Current direction:

- values have clear ownership
- resources are released deterministically
- scope-based cleanup is preferred
- explicit low-level operations may require `unsafe`
- ordinary programs should not require manual `alloc` / `free`

Example direction:

    fn process() {
        let data = Buffer<f32>(1024)
        compute(data)
    }

`data` should be automatically cleaned up when its lifetime ends.

The memory model will be designed incrementally and must remain simpler than Rust's full borrow-checking model unless stronger complexity is justified.

**Settled so far (KAI LANGUAGE M8A, executable as of M8B):** fixed-size arrays (`[T; N]`) are KAI value types -
`let b = a` and whole-array assignment are ordinary value copies, with no aliasing, no implicit sharing, and
no copy-on-write, and `[T; N]` is Copy-like exactly when its element type `T` is. This is real, native,
executable code as of KAI LANGUAGE M8B (post-alpha.2), including array function parameters/returns. This is a
narrow, concrete instance of the still-open direction above, not a declaration that the full ownership/
borrowing model is finalized - see TYPE_SYSTEM.md §19 for the complete rule and DESIGN_QUESTIONS.md for what
remains open.

**A second, narrower instance (KAI LANGUAGE M10A/M10B):** an immutable slice `[T]` is a NON-OWNING view -
KAI's first non-owning reference-like value, introduced without a general borrow checker. Rather than
designing full lifetime analysis, M10B enforces a deliberately restricted rule BY CONSTRUCTION: a slice may
only be built (via the explicit `slice(array)` builtin) from a direct local or parameter array binding, may
never be returned from a function, and may never be smuggled out through a returned aggregate that wraps one -
so a slice's backing storage is always guaranteed to dominate every use of it within the current function
invocation, with no dedicated checker pass required to prove it. This is explicitly NOT the "exact ownership
and borrowing model" this section still calls unfinalized above - it is a narrow, source-restricted special
case, simpler than Rust's own borrow checking by a wide margin, kept only as far as the immediate `slice(...)`
use case requires. See TYPE_SYSTEM.md's own "Slices" section for the complete rule and DESIGN_QUESTIONS.md for
what remains open (general slice-return safety, sub-slicing, mutable slices).

---

## 5. AI Semantic Tooling

KAI-CC should eventually expose its semantic knowledge through tooling.

Possible commands include:

    kai inspect User
    kai type expression
    kai refs calculate_total
    kai deps authenticate
    kai impact User.id

The compiler should be able to provide information without requiring an agent to manually read every relevant source file.

Example:

    $ kai impact User.id

    symbol: User.id

    defined:
      src/user.kai:8

    referenced:
      12 locations

    changing this symbol may affect:
      5 functions
      2 public APIs
      4 tests

Machine-readable equivalents should also be available.

---

## 6. Language Philosophy

KAI should be:

- statically typed
- compiled
- readable
- explicit when behavior matters
- concise when information can be inferred safely
- deterministic
- tooling-friendly
- suitable for systems programming
- suitable for large codebases

KAI should not become a collection of unrelated convenience features.

Every major language feature must justify its complexity.

---

## 7. Non-Goals for KAI 0.1

KAI 0.1 will NOT attempt to provide:

- classes
- inheritance
- async/await
- macros
- compile-time metaprogramming
- GPU programming
- automatic parallelization
- package registry
- web frameworks
- JIT compilation
- garbage collection
- self-hosting
- multiple code generation backends
- advanced borrow checking

These features may be explored in later versions.

---

## 8. Long-Term Goals

Potential long-term goals include:

- generics
- traits
- package management
- semantic agent API
- SIMD
- parallel programming
- GPU computing
- WebAssembly
- cross compilation
- self-hosted compiler
- compiler plugins
- rich profiling and debugging tools

These are directions, not commitments.

---

## 9. Self-Hosting

The first implementation of KAI-CC will be written in C++.

Once KAI is expressive and stable enough to implement its own compiler, a self-hosted compiler may be developed.

Possible bootstrap process:

    KAI-CC Stage 0
    C++ implementation
          |
          v
    compiles KAI compiler source
          |
          v
    KAI-CC Stage 1
    written in KAI
          |
          v
    compiles itself
          |
          v
    KAI-CC Stage 2

Self-hosting is a long-term milestone and must not influence KAI 0.1 development at the cost of simplicity.

---

## 10. Declarations, Scoping, and Name Resolution

KAI resolves names lexically, using ordinary block-structured scoping.

### Same-Scope Duplicates

A declaration conflicts only with another declaration in the same lexical scope.

    fn f() {
        let x = 1
        let x = 2
    }

is a compile-time error: `x` is declared twice in the same scope.

A function's parameters and the declarations in its outermost body block share one lexical scope:

    fn f(x: i32) {
        let x = 1
    }

is also a same-scope duplicate, not shadowing.

A `for` loop's variable and the declarations in its body's outermost block similarly share one lexical scope:

    for item in values {
        let item = 1
    }

is a same-scope duplicate.

### Nested Shadowing

A binding in a genuinely nested lexical scope - an `if`/`else if`/`else` body or a `while` body - may shadow an outer user binding:

    fn f() {
        let x = 1

        if true {
            let x = 2
        }
    }

is valid. Inside the `if` body, `x` refers to the inner declaration; outside it, `x` refers to the outer one. Name lookup always uses the innermost visible declaration.

    let item = 1

    for item in values {
    }

is valid for the same reason: the entire `for` construct - loop variable and body together - is one scope nested inside the scope that surrounds the loop.

### Initializer Visibility

A local binding does not become visible inside its own initializer. A declaration's initializer is resolved against the environment that exists before the new binding is introduced:

    let x = x

does not resolve the right-hand `x` to the binding being declared. If no outer `x` exists, this is an unresolved-name error. If an outer `x` exists in an enclosing scope, the right-hand `x` resolves to it, and the new `x` begins shadowing only after its own initializer has been resolved.

### Source-Order Visibility for Locals

Local declarations are not hoisted. A local is visible only to code that follows it within its own scope:

    let y = x
    let x = 1

leaves the first line's `x` unresolved, even though `x` is declared later in the same function. Reversing the order resolves it.

### Forward and Recursive Function References

Top-level function declarations in a source file are all collected before any function body is resolved. As a result, a function may refer to another top-level function declared later in the same file, and a function may refer to itself:

    fn main() {
        helper()
    }

    fn helper() {}

and

    fn fib(n: i32) -> i32 {
        return fib(n)
    }

are both valid regardless of declaration order.

### Member Names

For `object.member`, `object` participates in ordinary lexical name resolution. `member` does not: its meaning depends on the semantic type of `object` and is resolved separately, once member/type resolution exists.

---

## 11. Core Rule

KAI should not try to make AI agents better programmers through prompting.

KAI should reduce the number of opportunities an AI agent has to make incorrect changes through language design, static analysis, structured diagnostics, and semantic tooling.