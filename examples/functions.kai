fn add(a: i32, b: i32) -> i32 {
    return a + b
}

fn multiply(a: i32, b: i32) -> i32 {
    return a * b
}

fn greet(name: str) {
    print("Hello")
    print(name)
}

fn main() {
    let x = add(20, 22)
    let y = multiply(x, 2)

    greet("KAI")

    print(x)
    print(y)
}