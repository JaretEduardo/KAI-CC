fn main() {
    let numbers = [10, 20, 30, 40]

    print(numbers[0])
    print(numbers[3])

    for i in 0..4 {
        print(numbers[i])
    }

    mut counters = [0, 0, 0]

    for i in 0..3 {
        counters[i] = counters[i] + 1
    }

    for i in 0..3 {
        print(counters[i])
    }
}
